/**
 * Alpha function is a function that does nothing.
 *
 * @param {UknownType} input - Parameters for the function. // ✅️ exempted `jsdoc/valid-types`
 */
function alphaFunction ({
  first,
  second,
}) {
  // noop
}

export default {
  alphaFunction,
}
