/** @param {string} value */ // ❌️ singleLineTags:[lends, type, extends, inheritdoc, override] of `jsdoc/multiline-blocks`
function alphaFunction (value) {
  // noop
}

/** @param {*} input */ // ❌️ singleLineTags:[lends, type, extends, inheritdoc, override] of `jsdoc/multiline-blocks`
function betaFunction (input) {
  // noop
}

export default {
  alphaFunction,
  betaFunction,
}
