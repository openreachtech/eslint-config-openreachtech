'use strict'

const alpha = []

const beta = ['first', // ❌ consistent of `array-bracket-newline`
]

const gamma = [ // ❌ consistent of `array-bracket-newline`
  'first', 'second']

const delta = ['first', 'second', 'third', // ❌ consistent of `array-bracket-newline`
]

const epsilon = [
  'first',
  'second',
  'third',
  'fourth'] // ❌ consistent of `array-bracket-newline`

export default {
  alpha,
  beta,
  gamma,
  delta,
  epsilon,
}
