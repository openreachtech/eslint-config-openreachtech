/* eslint-disable max-classes-per-file */

class AlphaUtil { // ❌ { selector: 'Identifier[name=/.+Util$/]' } of `no-restricted-syntax`
  // noop
}

class BetaUtils { // ❌ { selector: 'Identifier[name=/.+Utils$/]' } of `no-restricted-syntax`
  // noop
}

export default {
  AlphaUtil,
  BetaUtils,
}
